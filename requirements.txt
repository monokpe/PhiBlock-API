# -- Core Web Framework --
fastapi
uvicorn[standard]
gunicorn
python-dotenv

# -- Database --
# Recommended for production over psycopg2-binary
# Requires build-essential and libpq-dev in Dockerfile
psycopg
psycopg2-binary # Required for SQLAlchemy's default driver

SQLAlchemy
alembic

# -- Caching & State --
# Used for rate limiting and idempotency
redis==5.0.1
sentry-sdk==1.40.0
stripe==8.1.0
celery
slowapi
strawberry-graphql[fastapi]

# Development dependencies
black
isort
mypy
pylint
flake8
flake8-docstrings
radon
vulture
bandit
pre-commit
types-redis
types-requests
types-PyYAML

# -- Authentication & Security --
python-jose[cryptography]
passlib[bcrypt]

# -- NLP/ML & Detection --
spacy
transformers
langchain
torch --index-url https://download.pytorch.org/whl/cpu
tiktoken # For accurate OpenAI token counting

# -- Testing --
httpx
pytest
pytest-asyncio
locust
